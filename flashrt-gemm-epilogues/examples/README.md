# Examples

This directory contains Hub-facing usage examples that do not depend on the
FlashRT serving runtime.

- `fp8_quant_epilogue_block.py`: minimal module for replacing BF16
  post-projection epilogue work with FlashRT FP8 quantization epilogue kernels.
