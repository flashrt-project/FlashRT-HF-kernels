# Benchmarks

These scripts are Hugging Face `kernels benchmark` workloads. Each file defines
one `kernels.benchmark.Benchmark` subclass and several named shape workloads.

`benchmark.py` compares GELU plus FP8 quantization, with and without bias,
against the equivalent PyTorch eager expression across decode and prefill
shapes.

`benchmark_bf16_gemm_bias_gelu.py` compares BF16 GEMM plus bias, with or
without GELU, against the equivalent PyTorch eager expression across decode,
small-batch, prefill, and wider projection shapes.

`benchmark_channel_scale.py` compares per-channel scaling plus FP8 quantization
against the equivalent PyTorch eager expression across the same quantization
shape suite.

These scripts expect `flashrt_gemm_epilogues` to be importable with its
generated `_ops` module. In normal use that means running them after loading or
installing a built HF kernel package. Running them directly from the source tree
with only `PYTHONPATH=flashrt-gemm-epilogues/torch-ext` is not sufficient,
because `_ops.py` is generated by `kernel-builder`.

Run through the HF benchmark CLI after building or loading the package:

```bash
kernels benchmark ./flashrt-gemm-epilogues --warmup 20 --iterations 100
```

For active development, use the ignored local source-extension benchmark in
`internal-tests/flashrt-gemm-epilogues/local_benchmark.py --suite`. That path
prints TFLOPS/GB/s and promote/watch/reject status without invoking the full
HF/Nix builder.

Preliminary local source-extension results are tracked in `RESULTS.md`.
