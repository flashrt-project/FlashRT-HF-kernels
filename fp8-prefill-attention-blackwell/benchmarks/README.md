# Benchmarks

Run `python benchmark.py` after loading a source or installed package. Public
results must report identical FP8 inputs, allocation policy, hardware, CUDA,
PyTorch, warmup and iteration count for FlashRT, eager SDPA and compiled SDPA.
