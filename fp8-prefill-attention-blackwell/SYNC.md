# Source synchronization

- Upstream: `flashrt-project/FlashRT`
- Source family: `csrc/attention/fmha_fp8_causal_gqa_sm120.{cu,cuh}`
- Local changes are restricted to the HF Tensor binding, validation, docs and
  package metadata. CUDA math changes require upstream parity revalidation.
