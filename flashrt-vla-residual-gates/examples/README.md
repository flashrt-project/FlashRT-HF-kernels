# examples

Run the minimal residual/gate example after installing `kernels` or after
adding a built artifact to `PYTHONPATH`:

```bash
python flashrt-vla-residual-gates/examples/joint3_residual_gates.py
python flashrt-vla-residual-gates/examples/joint3_residual_gates.py --compile
```
