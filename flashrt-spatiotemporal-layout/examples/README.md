# examples

Run the minimal spatiotemporal layout example after installing `kernels` or
after adding a built artifact to `PYTHONPATH`:

```bash
python flashrt-spatiotemporal-layout/examples/spatiotemporal_layout.py
python flashrt-spatiotemporal-layout/examples/spatiotemporal_layout.py --compile
```
