# Benchmarks

Run:

```bash
python benchmarks/benchmark.py --backend source --mode headline
```

Benchmarks compare FlashRT kernels with direct PyTorch eager reference
implementations for the same math and layout contract.
