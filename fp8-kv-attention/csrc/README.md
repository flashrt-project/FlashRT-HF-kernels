# Source

Native CUDA XQA source for BF16-Q + FP8-KV attention.

`attention/flashinfer_xqa_src/` is vendored FlashInfer XQA source already used
by FlashRT. `xqa_mha_configured.cu` fixes the public package instantiation.
