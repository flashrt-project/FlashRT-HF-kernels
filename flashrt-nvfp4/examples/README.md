# Examples

This directory contains Hub-facing usage examples that do not depend on the
FlashRT serving runtime.

- `nvfp4_scale_factor_layout.py`: minimal example for converting linear NVFP4
  scale-factor bytes to the CUTLASS Sm1xx swizzled layout.
