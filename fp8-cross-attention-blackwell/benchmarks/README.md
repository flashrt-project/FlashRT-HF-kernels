# Benchmark protocol

Compare the installed artifact with dequantized PyTorch eager,
`torch.compile`, and the corresponding native FlashRT entry using identical
inputs, scales, stream, warm-up, and CUDA-event timing.
