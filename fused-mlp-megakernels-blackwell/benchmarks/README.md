# Benchmark protocol

Benchmark after warm-up with CUDA events on one stream. Report PyTorch eager,
`torch.compile`, the Hub artifact, and the matching native FlashRT symbol. A
row is publishable only when its correctness gate passes.
