# examples

Run the minimal adaptive-norm example after installing `kernels` or after
adding a built artifact to `PYTHONPATH`:

```bash
python flashrt-adaptive-norms/examples/adaptive_norms.py
python flashrt-adaptive-norms/examples/adaptive_norms.py --compile
```
