# Benchmarks

Run source benchmark:

```bash
python vl-transformer-primitives/benchmarks/benchmark.py --backend source
```

Run installed artifact benchmark:

```bash
python vl-transformer-primitives/benchmarks/benchmark.py \
  --backend installed \
  --artifact vl-transformer-primitives/build/<variant>
```
