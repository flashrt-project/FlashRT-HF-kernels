# Scripts

This package currently uses repository-level release tooling:

- `scripts/prebuild_check.py`
- `.github/workflows/build-kernels-hf-jobs.yml`

Package-specific correctness and benchmark commands live under `tests/` and
`benchmarks/`.
