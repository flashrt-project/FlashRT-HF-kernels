[general]
name = "flashrt-smallm-gemm"
license = "Apache-2.0"
version = 1
backends = ["cuda"]

[general.hub]
repo-id = "flashrt/flashrt-smallm-gemm"

[torch]
src = [
  "torch-ext/torch_binding.cpp",
  "torch-ext/torch_binding.h",
]

[kernel.flashrt_smallm_gemm]
backend = "cuda"
depends = ["torch", "cutlass_4_0"]
include = ["csrc"]
# Restrict cuda-capabilities only after selecting source slices.
src = [
  # Add synced source files here before renaming this file to build.toml.
]
