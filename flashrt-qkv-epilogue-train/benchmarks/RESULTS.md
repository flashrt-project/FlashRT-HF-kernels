# Benchmark Results

CUDA optimized benchmark results are pending.

This v1 skeleton intentionally publishes reference/autograd APIs only. Do not use this package card to claim speedups until the optimized kernel implementation passes the acceptance gates.
